apiVersion: v1
kind: Pod
metadata:
  name: unsigned-alpine-test
  labels:
    test: signature-verification
spec:
  containers:
  - name: alpine
    image: alpine:latest
    command: ["sh", "-c", "echo 'This should fail!' && sleep 3600"]
  restartPolicy: Never
